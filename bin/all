#!/bin/bash

set -eu -o pipefail

DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )/.."
cd "$DIR"

find . -name metalink | cut -c3- | xargs -n1 dirname | xargs -n1 ./bin/watch "$S3_BLOBS"

git add .
git commit -m 'bump'
