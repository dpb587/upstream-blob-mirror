#!/bin/bash

set -eu -o pipefail

DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )/.."

cd "$DIR/repos"

find . -name .latest | xargs -n1 dirname | xargs -n1 ../bin/watch "$S3_BLOBS"

git add .
git commit -m 'bump'

../bin/mirror-repos
